[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python manage.py migrate --noinput && gunicorn tft_tracker.wsgi --bind 0.0.0.0:$PORT --workers 4 --threads 4 --timeout 60"
restartPolicyType = "on_failure"
